import os
from pathlib import Path

if os.name == "nt":
    os.add_dll_directory(str(Path(__file__).parent))
    # CUDA-enabled wheels link to the user's toolkit, matching native CUDA
    # applications. Python 3.8+ no longer searches PATH for dependent DLLs,
    # so explicitly register CUDA_PATH/bin when the toolkit is installed.
    cuda_path = os.environ.get("CUDA_PATH")
    if cuda_path:
        for cuda_bin in (Path(cuda_path) / "bin", Path(cuda_path) / "bin" / "x64"):
            if cuda_bin.is_dir():
                os.add_dll_directory(str(cuda_bin))

from sirius._sirius_ext import (
    Buffer,
    ApodizationType,
    Device,
    DeviceProperties,
    DeviceType,
    FFT,
    SIMParameters,
    SimFit,
    SimReconstructor,
    PixelType,
    PlanRigor,
    Stream,
    TiffCompression,
    TiffFile,
    TiffImageInfo,
    TiffInfo,
    TiffLayout,
    TiffLevel,
    built_with_cuda,
    built_with_nvtiff,
    cuda_available,
    cuda_device_count,
    device_properties,
    inspect_tiff,
    load_legacy_parameters,
    load_parameters,
    read_tiff,
    synchronize_device,
    save_parameters,
    to_device,
    BlendMode,
    MaskedNccOptions,
    StitchLayout,
    StitchOptions,
    TileMatch,
    TranslationResult,
    fuse_tiles,
    masked_ncc,
    next_fast_fft_size,
    optimize_tile_positions,
    plan_stitch,
    register_tile_pair,
    register_translation_masked,
    stitch_tiff_tiles,
    write_tiff,
)

__all__ = [
    "Buffer",
    "ApodizationType",
    "Device",
    "DeviceProperties",
    "DeviceType",
    "FFT",
    "SIMParameters",
    "SimFit",
    "SimReconstructor",
    "PixelType",
    "PlanRigor",
    "Stream",
    "TiffCompression",
    "TiffFile",
    "TiffImageInfo",
    "TiffInfo",
    "TiffLayout",
    "TiffLevel",
    "built_with_cuda",
    "built_with_nvtiff",
    "cuda_available",
    "cuda_device_count",
    "device_properties",
    "inspect_tiff",
    "load_legacy_parameters",
    "load_parameters",
    "read_tiff",
    "synchronize_device",
    "save_parameters",
    "to_device",
    "BlendMode",
    "MaskedNccOptions",
    "StitchLayout",
    "StitchOptions",
    "TileMatch",
    "TranslationResult",
    "fuse_tiles",
    "masked_ncc",
    "next_fast_fft_size",
    "optimize_tile_positions",
    "plan_stitch",
    "register_tile_pair",
    "register_translation_masked",
    "stitch_tiff_tiles",
    "write_tiff",
]
