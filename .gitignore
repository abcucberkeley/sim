# Build trees, generated files and local scratch. Trimmed from the generic
# GitHub Python template: only what this repository (CMake/C++/CUDA library,
# Qt app, nanobind wheel, Python worker) actually produces. `lib/`, `*.spec`
# and `*.manifest` are deliberately NOT ignored: they would hide legitimate
# directories and files. `.vscode/` is tracked.

# --- CMake / C++ -------------------------------------------------------------
build/
compile_commands.json
CMakeUserPresets.json
imgui.ini
wisdom*
*.o
*.obj
*.gch
*.pch
*.ilk
*.pdb
*.so
*.dylib
*.dll
*.a
*.lib
*.exe
*.out
*.app
*.dwo

# --- Local experiments (not part of the tree) --------------------------------
prototypes/
proto/
# Benchmark scratch space: generated TIFF datasets + on-the-fly cpp-tiff clone
/.bench_tmp/

# --- Python ------------------------------------------------------------------
__pycache__/
*.py[codz]
*$py.class
*.egg-info/
*.egg
.eggs/
dist/
wheels/
share/python-wheels/
.installed.cfg
MANIFEST
pip-log.txt
pip-delete-this-directory.txt
.ruff_cache/
.mypy_cache/
.pytest_cache/
.hypothesis/
.ipynb_checkpoints
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
coverage.xml
*.cover

# --- Environments ------------------------------------------------------------
.env
.envrc
.venv
venv/
env/
ENV/
.python-version

# --- Editors / OS ------------------------------------------------------------
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
tempCodeRunnerFile.py

# --- Containers --------------------------------------------------------------
# Apptainer/Singularity images
*.sif

# --- Logs --------------------------------------------------------------------
*.log
